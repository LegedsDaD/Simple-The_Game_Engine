# Simple Editor Packaging

This folder contains OS-specific PyInstaller specs for building the **Simple Editor**
cleanly on Windows, macOS, and Linux.

- `specs/windows.spec`
- `specs/macos.spec`
- `specs/linux.spec`

The shared build logic lives in `specs/_common.py`.

